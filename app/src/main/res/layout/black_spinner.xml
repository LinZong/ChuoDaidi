<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:background="@drawable/spinner_bg">
    <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
              style="@style/WelcomeTextViewStyle"
              android:id="@+id/Spinner_SelectedItem"
              android:paddingStart="5dp"
              android:paddingEnd="5dp"
              android:paddingTop="5dp"
              android:paddingBottom="5dp"
              android:text="测试傻逼东西"/>
    <ImageView android:layout_width="wrap_content"
               android:id="@+id/Spinner_ExpandItemGroupArrow"
               android:layout_height="wrap_content"
               android:src="@drawable/ic_keyboard_arrow_down_white_24dp"
               android:layout_marginEnd="4dp" android:layout_alignParentEnd="true"
               android:layout_marginTop="4dp"
               android:layout_alignTop="@+id/Spinner_SelectedItem"
               android:layout_marginBottom="4dp"
               android:layout_alignBottom="@+id/Spinner_SelectedItem"/>
    <android.support.v7.widget.RecyclerView
            android:id="@+id/Spinner_ItemList"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/spinner_bg"
            android:layout_below="@id/Spinner_SelectedItem"
    >
    </android.support.v7.widget.RecyclerView>
</RelativeLayout>